services:
  traefik:
    image: traefik:v3.1
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "${TRAEFIK_WEB_PORT:-80}:80"
      - "127.0.0.1:${TRAEFIK_DASHBOARD_PORT:-8080}:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
    networks:
      - edge

  front-service:
    image: ${FRONT_IMAGE}
    networks:
      - edge
    labels:
      - traefik.enable=true
      - traefik.http.routers.front.rule=PathPrefix(`/`)
      - traefik.http.routers.front.entrypoints=web
      - traefik.http.routers.front.priority=100
      - traefik.http.services.front.loadbalancer.server.port=${FRONT_INTERNAL_PORT}

  auth-service:
    image: ${AUTH_IMAGE}
    networks:
      - edge
    labels:
      - traefik.enable=true
      - traefik.http.routers.auth.rule=PathPrefix(`/api/auth`)
      - traefik.http.routers.auth.entrypoints=web
      - traefik.http.routers.auth.priority=200
      - traefik.http.routers.auth.middlewares=auth-strip
      - traefik.http.middlewares.auth-strip.stripPrefix.prefixes=/api/auth
      - traefik.http.services.auth.loadbalancer.server.port=${AUTH_INTERNAL_PORT}

networks:
  edge:
    name: infra-edge
